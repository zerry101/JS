let name="zishan"
arr=Array.from(name);
console.log(arr);

// FOREACH
num=['w','e','t','y'];
num.forEach((data)=>{
console.log(data);
})
    

// for of

// below will wrrite elements of num array
for(let i of num){
    console.log(i);
}

// below will wrrite index of num array
for(let i in num){
    console.log(i);
}
